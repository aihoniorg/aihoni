name: Build Android preview

on:
  push:
    branches: ['main']

jobs:
  build_android_preview:
    name: Build Android preview APK
    type: build
    params:
      platform: android
      profile: preview
